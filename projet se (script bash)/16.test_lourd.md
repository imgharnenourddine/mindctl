```
Tu es un expert Java/Spring Boot/React/Linux/DevOps.

Tu dois créer les fichiers du scénario test_lourd
pour le projet mindctl — orchestrateur DevOps Linux.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONTEXTE MINDCTL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

mindctl a un agent "depguard" qui lit docker-compose.yml
et extrait automatiquement avec ce code bash exact :

DB_TYPE=$(grep -i "image:" docker-compose.yml \
          | grep -iE "postgres|mysql|mongo" \
          | grep -oiE "postgres|mysql|mongo" | head -1)

DB_PORT=$(grep -A10 "$DB_TYPE" docker-compose.yml \
          | grep -oP '\d+:\d+' | head -1 | cut -d: -f1)

DB_USER=$(grep -iE "POSTGRES_USER|MYSQL_USER" \
          docker-compose.yml \
          | cut -d= -f2 | tr -d '"' | head -1)

DB_PASS=$(grep -iE "POSTGRES_PASSWORD|MYSQL_PASSWORD" \
          docker-compose.yml \
          | cut -d= -f2 | tr -d '"' | head -1)

DB_NAME=$(grep -iE "POSTGRES_DB|MYSQL_DATABASE" \
          docker-compose.yml \
          | cut -d= -f2 | tr -d '"' | head -1)

Puis écrit dans /tmp/mindctl_context.env :
DB_TYPE, DB_PORT, DB_USER, DB_PASS, DB_NAME

depguard vérifie aussi :
→ java installé + version
→ maven installé
→ node installé + version
→ npm installé
→ ports libres
→ RAM et disque

Les agents cleaner et analyzer lisent ce fichier
pour se connecter à la base Aiven via SSL.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INFOS CONNEXION AIVEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Host     : pg-7768e65-imgharnenourddine3-59b8.l.aivencloud.com
Port     : 26613
User     : avnadmin
Password : AVNS_PMEHEjjPC5jTatua5-F
Database : testdb_lourd
SSL      : obligatoire (sslmode=require)

Tables existantes dans testdb_lourd :
table_01 jusqu'à table_50
Chaque table : 110 lignes (100 normales + 10 doublons)
Colonnes : id, nom, email, valeur, categorie, date_col, statut

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STRUCTURE À CRÉER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

~/mindctl/exemple/test_lourd/
├── docker-compose.yml    ← conflit PORT intentionnel
├── Dockerfile            ← conflit python version
├── requirements.txt      ← conflit flask version
├── .env                  ← conflits multiples
├── nginx.conf            ← conflit port nginx
├── pom.xml               ← Spring Boot projet Java
├── src/
│   └── main/
│       └── java/
│           └── mindctl/
│               └── MindctlApplication.java
│               └── DataController.java
└── frontend/             ← React app
    ├── package.json
    ├── src/
    │   ├── App.jsx
    │   ├── components/
    │   │   ├── DataTable.jsx
    │   │   ├── StatsChart.jsx
    │   │   ├── ConflitsCard.jsx
    │   │   └── InsightPanel.jsx
    │   └── index.js
    └── public/
        └── index.html

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 1 : docker-compose.yml
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

version: '3'
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: avnadmin
      POSTGRES_PASSWORD: AVNS_PMEHEjjPC5jTatua5-F
      POSTGRES_DB: testdb_lourd
    ports:
      - "26613:5432"

  app:
    image: openjdk:11
    environment:
      POSTGRES_USER: avnadmin
      POSTGRES_PASSWORD: AVNS_PMEHEjjPC5jTatua5-F
      POSTGRES_DB: testdb_lourd
      PORT: 8000
    ports:
      - "8000:8080"
    depends_on:
      - db

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    depends_on:
      - app

CONFLIT 1 :
→ image: openjdk:11
→ pom.xml dira java 17 ← contradiction !

CONFLIT 2 :
→ PORT: 8000 dans docker-compose
→ .env dira PORT=9000 ← contradiction !

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 2 : Dockerfile
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FROM python:3.8
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python3", "-m", "flask", "run", "--host=0.0.0.0"]

CONFLIT intentionnel :
→ FROM python:3.8 ici
→ .env dira PYTHON_VERSION=3.11 ← contradiction !

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 3 : requirements.txt
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

flask==1.0.0
psycopg2-binary==2.8.0
requests==2.20.0

CONFLIT intentionnel :
→ flask==1.0.0 version très ancienne
→ .env dira FLASK_VERSION=2.0 ← contradiction !

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 4 : .env
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PORT=9000
PYTHON_VERSION=3.11
FLASK_VERSION=2.0
JAVA_VERSION=17
DB_HOST=pg-7768e65-imgharnenourddine3-59b8.l.aivencloud.com
DB_PORT=26613
DB_USER=avnadmin
DB_PASS=AVNS_PMEHEjjPC5jTatua5-F
DB_NAME=testdb_lourd

CONFLITS intentionnels :
→ PORT=9000      vs docker-compose PORT=8000
→ PYTHON_VERSION=3.11 vs Dockerfile python:3.8
→ FLASK_VERSION=2.0   vs requirements flask==1.0.0
→ JAVA_VERSION=17     vs docker-compose openjdk:11

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 5 : nginx.conf
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

events {}

http {
    server {
        listen 8080;

        location / {
            proxy_pass http://localhost:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api {
            proxy_pass http://localhost:9000;
        }
    }
}

CONFLITS intentionnels :
→ listen 8080 vs PORT=9000 dans .env
→ proxy_pass 8000 vs proxy_pass 9000
→ depguard détecte 3 ports différents :
   8080, 8000, 9000 ← incohérence totale !

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 6 : pom.xml ← Spring Boot
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Créer un pom.xml Spring Boot complet avec :

→ groupId: com.mindctl
→ artifactId: mindctl-test-lourd
→ version: 1.0.0
→ Java version: 17
→ Spring Boot version: 3.1.0
→ Dépendances :
   spring-boot-starter-web
   spring-boot-starter-data-jpa
   postgresql driver
   lombok

CONFLIT intentionnel :
→ pom.xml dit java 17
→ docker-compose dit openjdk:11 ← contradiction !
→ depguard détecte → LLM propose correction

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 7 : MindctlApplication.java
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

package mindctl;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MindctlApplication {
    public static void main(String[] args) {
        SpringApplication.run(MindctlApplication.class, args);
    }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 8 : DataController.java ← API REST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Créer un controller Spring Boot avec CES ROUTES :

━━━ ROUTE 1 : GET /api/tables ━━━
→ Liste tous les fichiers /tmp/mindctl_*_clean.csv
→ Extrait les noms des tables
→ Retourne JSON :
{
  "tables": ["table_01","table_02",..."table_50"]
}

━━━ ROUTE 2 : GET /api/csv/{table} ━━━
→ Lit /tmp/mindctl_{table}_clean.csv
→ Parse le CSV
→ Retourne JSON :
{
  "table": "table_01",
  "total_lignes": 95,
  "colonnes": ["id","nom","email","valeur","categorie","date_col","statut"],
  "donnees": [...]
}

━━━ ROUTE 3 : GET /api/stats ━━━
→ Lit /var/log/mindctl/reports/last_data.txt
→ Retourne :
{
  "stats": "contenu complet"
}

━━━ ROUTE 4 : GET /api/depguard ━━━
→ Lit /var/log/mindctl/reports/last_depguard.txt
→ Parse les blocs séparés par "---"
→ Retourne :
{
  "conflits": [
    {
      "niveau": "CRITIQUE",
      "description": "...",
      "fichier": "...",
      "ligne_avant": "...",
      "ligne_apres": "..."
    }
  ],
  "total": 5
}

━━━ ROUTE 5 : GET /api/insight ━━━
→ Lit /var/log/mindctl/reports/insight_*.txt
→ Prend le plus récent
→ Retourne :
{
  "correlation": "contenu complet du rapport insight"
}

━━━ CONFIGURATION Spring Boot ━━━
→ Port : 8080
→ CORS activé pour React (localhost:3000)
→ Lire les fichiers avec Java NIO (Path, Files)
→ Gérer les cas fichiers inexistants

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 9 : frontend/package.json ← React
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{
  "name": "mindctl-dashboard",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "chart.js": "^4.0.0",
    "react-chartjs-2": "^5.0.0",
    "axios": "^1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "proxy": "http://localhost:8080"
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FICHIER 10 : frontend/src/App.jsx ← React principal
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Créer App.jsx avec 4 COMPOSANTS :

━━━ COMPOSANT 1 : DataTable.jsx ━━━
→ Reçoit props : table (string)
→ Appelle GET /api/csv/{table} avec axios
→ Affiche tableau HTML avec :
   → en-tête coloré bleu foncé
   → lignes alternées
   → pagination (20 lignes par page)
   → badge "✅ X lignes nettoyées"
   → badge "🗑️ Y doublons supprimés"

━━━ COMPOSANT 2 : StatsChart.jsx ━━━
→ Appelle GET /api/stats avec axios
→ Parse le contenu pour extraire nulls par colonne
→ Affiche avec react-chartjs-2 :

  Chart 1 — Bar chart horizontal :
  → "Valeurs nulles par colonne"
  → couleur rouge pour nulls élevés
  → couleur verte pour nulls faibles

  Chart 2 — Line chart :
  → "Évolution des valeurs par table"
  → une ligne par métrique (min, max, moyenne)

  Chart 3 — Doughnut :
  → "Qualité des données globale"
  → sections : propres / doublons / nulls

━━━ COMPOSANT 3 : ConflitsCard.jsx ━━━
→ Appelle GET /api/depguard avec axios
→ Pour chaque conflit affiche une carte :

  ┌─────────────────────────────────────┐
  │ 🔴 CRITIQUE  |  📄 Dockerfile       │
  │                                     │
  │ openjdk:11 dans docker-compose      │
  │ mais java 17 dans pom.xml           │
  │                                     │
  │ ❌ FROM openjdk:11                  │
  │ ✅ FROM openjdk:17                  │
  └─────────────────────────────────────┘

  → CRITIQUE → fond rouge sombre
  → WARNING  → fond orange sombre
  → Compteur total conflits en haut

━━━ COMPOSANT 4 : InsightPanel.jsx ━━━
→ Appelle GET /api/insight avec axios
→ Affiche le rapport de corrélation LLM :
  → icône 🧠 en grand
  → texte formaté dans une carte sombre
  → bouton "Rafraîchir" pour recharger

━━━ App.jsx principal ━━━
→ Sidebar avec navigation :
   🗄️  Données
   📊 Statistiques
   ⚠️  Conflits DevOps
   🧠 Insight LLM

→ Header :
   "🧠 mindctl — Dashboard test_lourd"
   "Mode Threads — 50 tables — 5 conflits"

→ Sélecteur de tables :
   dropdown avec table_01 à table_50
   → sélection → charge DataTable

━━━ STYLE ━━━
→ Thème sombre professionnel
→ Sidebar fixe à gauche
→ Contenu à droite
→ Animations de chargement (spinner)
→ Responsive

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMMANDES BASH POUR CRÉER LES FICHIERS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

mkdir -p ~/mindctl/exemple/test_lourd/src/main/java/mindctl
mkdir -p ~/mindctl/exemple/test_lourd/frontend/src/components
mkdir -p ~/mindctl/exemple/test_lourd/frontend/public

cat > ~/mindctl/exemple/test_lourd/docker-compose.yml << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/Dockerfile << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/requirements.txt << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/.env << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/nginx.conf << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/pom.xml << 'EOF'
[contenu complet Spring Boot]
EOF

cat > ~/mindctl/exemple/test_lourd/src/main/java/mindctl/MindctlApplication.java << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/src/main/java/mindctl/DataController.java << 'EOF'
[contenu complet avec les 5 routes]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/package.json << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/src/App.jsx << 'EOF'
[contenu complet React]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/src/components/DataTable.jsx << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/src/components/StatsChart.jsx << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/src/components/ConflitsCard.jsx << 'EOF'
[contenu]
EOF

cat > ~/mindctl/exemple/test_lourd/frontend/src/components/InsightPanel.jsx << 'EOF'
[contenu]
EOF

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VÉRIFICATION FINALE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Donne ces commandes à la fin :

# Compiler Spring Boot
cd ~/mindctl/exemple/test_lourd
mvn clean install

# Lancer Spring Boot
mvn spring-boot:run

# Dans un autre terminal — lancer React
cd ~/mindctl/exemple/test_lourd/frontend
npm install
npm start

# Tester les routes Spring Boot
curl http://localhost:8080/api/tables
curl http://localhost:8080/api/csv/table_01
curl http://localhost:8080/api/stats
curl http://localhost:8080/api/depguard
curl http://localhost:8080/api/insight

# Ouvrir React
http://localhost:3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
POINTS IMPORTANTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Spring Boot lit les fichiers produits par mindctl
   → pas de connexion directe à la base
2. Les CSV sont dans /tmp/mindctl_*_clean.csv
3. Les rapports sont dans /var/log/mindctl/reports/
4. insight prend le fichier le plus récent :
   insight_*.txt → trier par date → prendre dernier
5. React utilise proxy vers Spring Boot port 8080
6. CORS configuré pour localhost:3000
7. Gérer les cas fichiers inexistants
8. Les 5 conflits doivent être clairement visibles
   dans le dashboard React pour la démo```